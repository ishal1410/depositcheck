import type { NextConfig } from 'next';

// ponytail: empty on purpose. Defaults cover this app; add config when
// something actually needs it.
const nextConfig: NextConfig = {};

export default nextConfig;
